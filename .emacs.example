;;; .emacs --- the init file

;;; Commentary:

;; Simplified init file.
;;
;; This uses `org-babel-load-file' to create a Lisp script.

;;; Code:

(defvar walheimat-emacs-config-default-path
  (expand-file-name "emacs-config" user-emacs-directory))

(let ((gc-cons-threshold most-positive-fixnum))
  (org-babel-load-file
   (expand-file-name "README.org" walheimat-emacs-config-default-path)))

;;; .emacs ends here
